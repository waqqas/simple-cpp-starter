cmake_minimum_required(VERSION 3.10)
project(exercise_4)

# Create an executable for enum.c
add_executable(enum_c enum.c)

# Create an executable for pass_by_reference.cpp
add_executable(enum_cpp enum.cpp)
